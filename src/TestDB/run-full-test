#!/bin/bash -e

if [ $# -ne 1 ] ; then
   echo >&2 "Usage: $0 mariadb|mongodb|mysql|postgresql"
   exit 1
fi


HOSTNAME=`hostname -s`
if [ "${HOSTNAME}" != "localhost" -o \
     "${HOSTNAME}" != "dbtest"    -o \
     "${HOSTNAME}" != "midtstuen" ] ; then
   echo >&2 "Hostname is not \"localhost\" or \"dbtest\". This script assumes to be run on a TEST SYSTEM only, since it replaces (i.e. erases) existing installations of PostgreSQL, MariaDB/MySQL and MongoDB!"
   echo >&2 "Stopping here."
   exit 1
fi


./0-make-users.conf ~/users.conf

./4-uninstall-database  $1 --force-removal-without-confirmation 
./1-install-database    $1 ~/users.conf
./2-initialise-database $1 ~/users.conf --force-removal-without-confirmation
./3-test-database       $1 ~/users.conf
