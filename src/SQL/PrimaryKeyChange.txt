ALTER TABLE Traceroute DROP CONSTRAINT traceroute_pkey;
ALTER TABLE Traceroute ADD COLUMN Round INTEGER NOT NULL DEFAULT 0;
ALTER TABLE Traceroute ADD PRIMARY KEY(TimeStamp,FromIP,ToIP,Round,HopNumber);
